// Test stub for the `server-only` package. Under Next.js this import throws if
// pulled into a client bundle; in the vitest (Node) runner it's a harmless
// no-op so server modules can be unit-tested in isolation.
export {};
